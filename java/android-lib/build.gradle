apply plugin: 'com.android.library'

dependencies {
	compile project(':lib')
}

android {
	compileSdkVersion 23
	buildToolsVersion '26.0.1'

	defaultConfig {
		minSdkVersion 7
		targetSdkVersion 23
		versionCode 1
		versionName '1.0'
	}

	buildTypes {
		release {
			minifyEnabled false
		}
	}
}
