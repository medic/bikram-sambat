apply plugin: 'com.android.library'

dependencies {
	compile project(':lib')
}

android {
	compileSdkVersion 26
	buildToolsVersion '27.0.1'

	defaultConfig {
		minSdkVersion 26
		targetSdkVersion 26
		versionCode 1
		versionName '1.0'
	}

	buildTypes {
		release {
			minifyEnabled false
		}
	}
}
